kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: {{ include "cas-reporting.fullname" . }}-intra-release-by-name
  labels: {{ include "cas-reporting.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: {{ .Release.Name }}
  ingress:
  - from:
    - podSelector:
        matchLabels:
          release: {{ .Release.Name }}
    - podSelector:
        matchLabels:
          app.kubernetes.io/name: {{ .Release.Name }}
